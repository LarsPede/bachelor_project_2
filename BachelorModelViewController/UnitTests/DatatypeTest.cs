using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using BachelorModelViewController.Models;

namespace BachelorModelViewController.UnitTests
{
    public class DatatypeTest
    {
        public DatatypeTest() { }
        public void run()
        {
            //Atomic a1 = new Atomic("string", typeof(string));
            //Atomic a2 = new Atomic("int", typeof(int));
            //Atomic a3 = new Atomic("double", typeof(double));
            //Atomic a4 = new Atomic("bool", typeof(bool));
            //Atomic a5 = new Atomic("float", typeof(float));
            //Atomic a6 = new Atomic("byte", typeof(byte));

            //Composite comp = new Composite(new List<Datatype>(new Datatype[] { a1, a2, a3 }));

            //Dictionary<int, Datatype> d = new Dictionary<int, Datatype>();
            //d.Add(0, a4);
            //d.Add(1, a5);
            //d.Add(2, comp);
            //d.Add(3, a6);

            //System.Console.WriteLine(d.ToString());
        }
    }
}
